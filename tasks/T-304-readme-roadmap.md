# T-304 · README / Roadmap 收尾

- 难度:⭐
- 状态:todo
- 波次:可与 T-305 并行(文件集不相交)。
- 允许修改的文件(白名单,严格遵守):
  - `README.md`
- 禁改:其他一切(本卡纯文档)。

## 背景

第三批(T-301~T-303)已交付:消息列表分页、表格渲染改 Compose 原生、拍照直拍 Compose 内整合。README 的 Roadmap 里这三项仍未勾选,且功能清单对"表格渲染""相机"的描述已落后于代码。

## 任务

1. `README.md` 的 **Roadmap** 段落,把这三项前面的 `- [ ]` 改为 `- [x]`:
   - 消息列表分页(超长会话)
   - 表格渲染升级为横向滚动视图的 Compose 原生版
   - 图片输入支持拍照直拍的 Compose 内整合
2. 「其他功能」清单核对与订正(与实际一致,例如拍照入口已从隐藏长按变为 📷 菜单、表格已是 Compose 渲染)。
3. 不改任何代码;不碰 CHANGELOG(那是 T-305)。

## 验收标准

- 文档描述与 main 分支代码一致;无代码改动。
- push 后 CI(全量单测)仍全绿(纯文档改动不应影响)。

## 交付记录

(完成后填写:认领人 / commit / 关键决策)
